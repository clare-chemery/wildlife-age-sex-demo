# wildlife-demographics
Machine learning project to extract sex (male/female) and age (young/old) characteristics of certain species using camera trap images.

## Set up

To run this project, install `uv` run `uv sync` to install the necessary dependencies in your preferred environment.

...
